rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    match /users/{userId}//{todoId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}